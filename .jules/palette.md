## 2025-05-20 - ThemeToggle ARIA Label
**Learning:** Icon-only buttons lacking ARIA labels are a common accessibility issue. Even simple interactions like theme toggling require an accessible name for screen readers.
**Action:** Always ensure any icon-only `<Button>` uses the `aria-label` attribute to describe its function explicitly.
